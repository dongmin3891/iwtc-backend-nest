# 후보 삭제 정책

작성일: 2026-09-10

## 1. 결론

후보는 물리 삭제하지 않고 `Candidate.deletedAt`을 사용하는 소프트 삭제로 처리한다.

- 과거 `GamePlacement`와 `Comment` 행은 보존한다.
- `deletedAt IS NULL`인 후보만 현재 사용 가능한 후보로 본다.
- 삭제된 후보는 공개 게임, 새 게임 결과 제출, 공개 랭킹, 관리 목록, 수정, 새 댓글 작성에서 제외한다.
- 기존에 저장된 게임 결과를 같은 `playId`로 재요청하는 경우에는 멱등성을 위해 당시 결과를 그대로 반환할 수 있다.
- 댓글 목록은 월드컵 전체 대화 기록이므로 후보가 삭제되어도 기존 댓글을 유지한다. 댓글 자체가 삭제된 경우에만 현재처럼 목록에서 제외한다.
- 후보 삭제 시 연결된 미디어는 즉시 삭제하지 않는다.

기존 Spring 서버도 후보에 `softDelete` 값을 두고 기본 조회에서 삭제 후보를 제외했다. 신규 NestJS 서버는 기존 DB를 이관하지 않지만, 과거 결과와 댓글을 보존해야 하는 현재 Prisma 관계에도 소프트 삭제가 가장 잘 맞는다.

## 2. 현재 구조에서 물리 삭제를 사용하지 않는 이유

현재 후보는 다음 데이터의 기준점이다.

- `GamePlacement.candidateId`는 후보를 `ON DELETE NO ACTION`으로 참조한다.
- `Comment.candidateId`는 후보를 `ON DELETE CASCADE`로 참조한다.
- 후보는 `MediaFile`을 선택적으로 참조한다.

따라서 후보를 물리 삭제하면 다음 문제가 생긴다.

1. 게임 결과가 한 건이라도 있는 후보는 외래 키 때문에 삭제가 실패한다.
2. 게임 결과가 없더라도 댓글은 함께 삭제되어 과거 대화 기록이 사라진다.
3. 후보의 누적 점수와 당시 게임 결과를 감사하거나 장애를 분석할 근거가 사라진다.
4. 프론트엔드의 삭제 요청 성공 여부가 후보의 과거 사용 여부에 따라 달라진다.

삭제를 `deletedAt` 갱신으로 통일하면 과거 참조를 유지하면서 사용자 화면에서는 후보를 제거할 수 있다.

## 3. 데이터 보존 정책

### Candidate

- `deletedAt: DateTime?`을 추가한다.
- `null`이면 활성 후보, 값이 있으면 삭제 후보로 해석한다.
- 삭제할 때 `visibleType`을 변경하지 않는다. 삭제 당시 상태를 보존하고 `deletedAt`이 공개 여부보다 우선한다.
- 첫 구현에는 복구 API와 영구 삭제 작업을 포함하지 않는다.

### GamePlacement와 GamePlay

- 기존 행을 수정하거나 삭제하지 않는다.
- 삭제 후보가 받은 과거 점수도 DB에는 그대로 둔다.
- 새 게임 결과에는 삭제 후보를 사용할 수 없다.
- 이미 저장된 `playId`의 동일 요청은 기존 결과를 반환한다. 이 응답에는 삭제된 후보가 포함될 수 있는데, 이는 결과 멱등성과 과거 결과 보존을 위한 예외다.

### Comment

- 후보가 삭제되어도 기존 댓글 행은 삭제하거나 소프트 삭제하지 않는다.
- 월드컵 댓글 목록은 기존처럼 `Comment.deletedAt IS NULL`인 댓글을 반환한다. 연결 후보가 이후 삭제되었는지는 목록 노출 조건으로 사용하지 않는다.
- 삭제 후보에는 새 댓글을 작성할 수 없다.
- 후보 삭제와 댓글 삭제는 서로 다른 사용자 행동으로 취급한다.

### MediaFile

- 후보 삭제 트랜잭션에서 미디어를 삭제하지 않는다.
- 미디어가 다른 후보에서 재사용될 수 있고, 기존 게임 결과의 표시나 추후 감사에 필요할 수 있다.
- 실제 오브젝트 삭제는 향후 영구 삭제·미참조 미디어 정리 정책에서 별도로 다룬다.
- 현재 공개 미디어 조회 API의 동작은 후보 삭제 단계에서 변경하지 않는다.

## 4. API별 삭제 후보 처리

| 영역 | 처리 방식 |
| --- | --- |
| 공개 월드컵 목록의 미리보기 후보 | `PUBLIC`이면서 `deletedAt IS NULL`인 후보만 사용 |
| 공개 월드컵 목록 자체 | 월드컵의 `visibleType` 기준을 유지한다. 활성 공개 후보가 2개 미만이어도 이 단계에서 목록에서 숨기지 않음 |
| 플레이 가능한 라운드 | `PUBLIC`이면서 `deletedAt IS NULL`인 후보 수로 계산 |
| 게임 대진 후보 | `PUBLIC`이면서 `deletedAt IS NULL`인 후보만 반환 |
| 새 게임 결과 저장 | 요청 후보가 모두 `PUBLIC`이고 `deletedAt IS NULL`일 때만 허용 |
| 기존 `playId` 결과 재요청 | 먼저 저장된 결과를 그대로 반환하여 멱등성 유지 |
| 공개 랭킹 | 활성 공개 후보만 노출하고, 해당 후보의 과거·현재 점수를 모두 합산 |
| 관리 후보 목록 | `deletedAt IS NULL`인 공개·비공개 후보만 반환 |
| 후보 수정 | `deletedAt IS NULL`인 소유 후보만 허용 |
| 후보 삭제 | 소유권과 활성 후보를 확인하고 `deletedAt` 기록 |
| 댓글 목록 | 삭제되지 않은 댓글은 연결 후보 삭제 여부와 관계없이 유지 |
| 댓글 작성 | 활성 공개 후보에만 허용 |

공개 월드컵에 활성 공개 후보가 2개보다 적으면 현재 계약대로 월드컵 목록에는 보일 수 있지만 `available-rounds`는 `400 Bad Request`를 반환한다. 공개 목록에서 이런 월드컵을 숨길지는 후보 삭제와 분리된 제품 정책으로 둔다.

## 5. 삭제 API 계약

- 경로: `DELETE /api/me/game-contents-manage/world-cups/{worldCupId}/contents/{contentsId}`
- 인증: access token 필수
- 성공: `204 No Content`
- 인증 없음 또는 유효하지 않은 토큰: `401 Unauthorized`
- 소유하지 않은 월드컵 또는 존재하지 않는 월드컵: `404 Not Found`
- 다른 월드컵의 후보, 존재하지 않는 후보, 이미 삭제된 후보: `404 Not Found`
- 같은 후보를 동시에 또는 반복해서 삭제하면 한 요청만 `204`, 나머지는 `404`가 되도록 한다.

다른 회원의 월드컵과 후보 존재 여부를 구분해서 노출하지 않는 기존 관리 API 원칙을 유지한다.

## 6. Prisma 스키마 영향

다음 구현 단계에서 스키마와 migration에 아래 변경이 필요하다.

1. `Candidate`에 `deletedAt DateTime? @map("deleted_at") @db.Timestamptz(3)` 추가
2. 후보 활성 조회를 위한 `(world_cup_id, deleted_at, visible_type)` 인덱스 추가
3. 기존 `(world_cup_id, visible_type)` 인덱스는 새 인덱스로 대체
4. `Comment.candidate`의 `onDelete`를 `Cascade`에서 `NoAction`으로 변경해 실수로 후보를 물리 삭제할 때 댓글이 사라지지 않도록 보호
5. `GamePlacement.candidate`의 `NoAction`은 유지
6. 후보와 `MediaFile` 관계의 `SetNull`은 유지

`@@unique([worldCupId, sortOrder])`도 유지한다. 삭제 후보의 `sortOrder`를 재사용하거나 전체 순서를 다시 매기지 않는다. 새 후보 생성 시 마지막 순번을 찾는 현재 조회에는 삭제 후보도 포함해야 고유 제약 충돌 없이 계속 증가한다.

## 7. 서비스 조회 변경 지점

구현 시 다음 위치에 활성 후보 조건을 빠짐없이 반영해야 한다.

- `WorldCupsService.findAll`: 미리보기 후보
- `WorldCupsService.findAvailableRounds`: 후보 수
- `WorldCupsService.findContents`: 후보 수와 실제 대진 후보
- `WorldCupsService.saveGameResult`: 새 결과의 후보 검증
- `WorldCupsService.findGameResultContents`: 공개 랭킹 후보
- `ManageWorldCupContentsService.updateOne`: 수정 대상 후보
- `ManageWorldCupContentsService.findAll`: 관리 목록 후보
- `CommentsService.create`: 댓글 작성 대상 후보

다음 조회는 의도적으로 삭제 후보를 포함하거나 후보 조건을 추가하지 않는다.

- 후보 생성의 마지막 `sortOrder` 조회: 삭제 후보 순번도 보존
- 이미 저장된 `playId` 결과 조회: 과거 결과와 멱등성 보존
- 월드컵 댓글 목록: 기존 댓글 보존

## 8. 동시성 원칙

- 삭제는 `where: { id, worldCupId, deletedAt: null }` 조건의 단일 조건부 갱신으로 처리하고 갱신 건수를 확인한다.
- 수정도 최종 후보 갱신에서 `deletedAt: null` 조건을 다시 확인해야 한다.
- 수정 중 삭제가 먼저 완료되면 수정 트랜잭션은 실패하고 미디어 변경도 롤백되어야 한다.
- 수정이 먼저 완료되고 삭제가 뒤따르면 두 요청 모두 성공할 수 있으며 최종 상태는 삭제 상태다.
- 게임 화면에서 후보를 받은 뒤 저장 전에 후보가 삭제되면 새 결과 저장은 실패한다. 서버가 게임 시작 시점의 후보를 별도로 고정하지 않는 현재 구조에서는 안전한 쪽인 거부를 선택한다.

## 9. 구현 순서

작업을 작게 나누어 다음 순서로 진행한다.

1. Prisma 스키마와 migration만 추가하고 생성·검증한다. (완료)
2. 공개 월드컵 목록 미리보기와 플레이 가능한 라운드 계산에 활성 후보 조건을 추가한다. (완료)
3. 게임 대진 후보 조회에 활성 후보 조건을 추가한다.
4. 새 게임 결과 저장의 후보 검증에 활성 후보 조건을 추가한다.
5. 공개 랭킹에 활성 후보 조건을 추가한다.
6. 관리 목록과 후보 수정에 활성 후보 조건을 추가한다.
7. 댓글 작성에 활성 후보 조건을 추가한다.
8. 소유자 전용 후보 삭제 서비스와 단위 테스트를 추가한다.
9. DELETE 컨트롤러와 API 통합 테스트를 추가한다.
10. 프론트엔드 삭제 저장 흐름을 자동화 테스트로 확인한다.
11. 실제 HTTP·PostgreSQL과 브라우저에서 삭제·재접속을 확인한다.
